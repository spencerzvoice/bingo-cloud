# 2026-09-22

## IMG feature-spots draft — wrong-mailbox bug found and fixed
- Created feature-spots pitch draft to Ian via the generic Gmail connector (`mcp__a3d646f8...create_draft`) — it silently saved to a different mailbox, not spencer@spencerzvoice.com. Spencer couldn't find it in his drafts box because it was never there.
- Root cause: that connector has no account-selection parameter and doesn't default to spencer@spencerzvoice.com. This is exactly the "generic Gmail connector" AGENTS.md/CLAUDE.md already warn to avoid for his work — should have used FGAC from the start for the write, not just the read-check.
- Spencer, 09-22: he only has one email as far as Bingo's concerned — spencer@spencerzvoice.com. Scrubbed other-mailbox references from MEMORY.md and reference/outreach-pipeline.md.
- Fix: deleted the wrong draft, recreated it correctly via FGAC `google_api_modify` (POST gmail/v1/users/me/drafts, account spencer@spencerzvoice.com). New draft id `1a0c8df4884356e1`, confirmed present via FGAC.
- LESSON: the "use FGAC not the generic connector" rule applies to Gmail *writes* (drafts/send), not just reads. Update AGENTS.md/CLAUDE.md wording next prune to say so explicitly.

## Feature-spots pitch sent
- No budget-fishing line added (Spencer asked, Bingo advised against — undercuts the stated €1,150, repeats the Jamie £50 pattern).
- Sent 2026-09-22 to Ian.Henderson@img.com only, msg id `1a0c8e790d2c9453`. Awaiting reply on volume/frequency + whether €1,150/session holds.

## Found the "Re-Voicing Material Retrieval" work Bingo had lost track of — correction
Spencer asked about Funnel B assets/recording. Bingo initially said nothing existed (checked Gmail drafts + a Temp scratch folder, both real but incomplete pictures) — **wrong**, and Spencer pushed back correctly. Full trace: git history/memory had zero record → found an empty `Temp\claude\revoice_captions\` scaffold (9/18, dead end) → found the real thing on live Gmail (Spencer's personal→business self-email, 9/10, subject "Funnel C Copy," 11 script .docx attached) → traced "Client Outreach" (Desktop shortcut) to **`D:\My Drive\Client Outreach\`**, a Google-Drive-synced local folder this session had never looked at. That's where it all actually lives.
- **What's really there, verified by directory listing just now:**
  - `Funnel B/` — 8 of 9 target companies (Atlassian, Asana ×2 videos, Canva, ClickUp, Grammarly, HubSpot ×2, monday.com, Zoom). Missing: Airtable. Each has: source `.mp4`, a vocals-stripped `..._no_vocals_split_by_lalalai.mp4`, a pre-built `<Client> Project/` Ableton session (`.als` + `Samples/Recorded/` empty), a `Ready to Link to Drafted Email/` folder (empty — nothing finished yet).
  - `Funnel C/` — 10 companies / 11 videos (Coursera ×2, Discovery Education ×2, Instructure, Kaplan UK, LearningMate, LinkedIn Learning, Skillshare, Udemy, Masterclass); Big Think intentionally dropped (documentary segment, no clean single-VO script). Same per-client structure, plus `<Client>_script.txt` transcripts. **Udemy's Ableton backup is timestamped 2026-09-11 09:18** — matches what Spencer remembered.
  - `Ableton Template Project/` at the root — the actual template both funnels' per-client projects were built from.
  - Scripts for Funnel C were also emailed to spencer@spencerzvoice.com 2026-09-10 (11 .docx, one per spot) — real, verified, still in Gmail.
- **What's NOT done:** nothing recorded yet in any `Samples/Recorded/` folder (checked several — all empty). The 16 Funnel B Gmail drafts (see 2026-09-22 session) still carry generic, non-specific hooks ("I've been going through [Company]'s video content...") — they don't yet name the actual source video, even though we now know exactly which video each one is (e.g. Atlassian = "Using @Jira in Microsoft Teams," Asana = "How Washmen and Cloudfresh built an AI-powered ops hub" + "What's new in Asana August 2026," HubSpot = "Meet Agent Hub" + "What is HubSpot," etc. — full list in the Drive folder). Worth rewriting the hooks to name the real video before sending.
- **Why this got lost:** the work was done inside two archived chat sessions ("Funnel B," created 2026-08-31; "Re-voicing Material Retrieval task setup," created 2026-09-10) as ad hoc tool use — never turned into a committed skill file, never logged to `MEMORY.md`/`memory/`, and the output folder (`D:\My Drive\Client Outreach\`) was never referenced anywhere in this repo. No scheduled task or cron job for it currently exists (checked `list_scheduled_tasks` + `CronList` — both empty), so it is not actually "scheduled" right now despite the name.
- **Open follow-up:** turn this into a real, durable skill (`.claude/skills/revoice-material/SKILL.md`) describing the pipeline (source video → vocal split via lalal.ai → NOVOX pair + script → Ableton project from the template → drop in `Client Outreach/<Funnel>/<Client>/`) so it survives instead of living only in one chat's tool calls. Not built yet — flagged, not done.
