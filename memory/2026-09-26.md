# 2026-09-26
- Lift pricing re-settled (Spencer): proportional by length, % of :30 parent: :15 = 75%, :06 = 30%, tag = 20%. Replaces 09-25 'parent minus 25-50%' rule. Worked example: 3 lifts (:15 + two :06) on $2,000 national radio :30 = $2,000 + $1,500 + $600 + $600 = $4,700. Updated quick-quote.md, vo-pricing skill, MEMORY.md, ~/.claude/agents/bingo.md.
- **Diagnosed the tracker digest bug Spencer flagged (Becca Winkler false "4.5 months silence").** Root cause found and fixed — see `reference/outreach-pipeline.md` hazard #6 for full detail: her stored email had a typo (`.co` not `.com`), the daily "VO Outreach Pipeline Daily Update" routine (trig_01PTnXAXdmvLjyqBCnsgPXVz, runs 7am UTC daily) queried Gmail with the broken address, got zero hits, and confidently asserted "not supported by Gmail" instead of flagging the address as unverified. Live Gmail (this session) confirmed the real history: Jul 31 reply + a live Sep 18 OOO auto-reply (back 9/28) — 8 days silent, not 4.5 months. Also found the same cross-contamination bug (hazard #3) recurring same-run: a stray Becca Winkler/Xpedition sentence landed in Ryan Lago's (Shadow Lion) Notes. Fixed both rows live in the Sheet (row 39 email + Last Contact Date + Next Action + Notes; row 72 Notes trimmed) — re-read after write to confirm. **The routine's prompt itself isn't in this repo** (it's a Claude Code Remote Routine, not code here) — the prompt logic is sound (it does require live Gmail checks), the failure was the address typo + the executing session overclaiming a negative instead of flagging it. Worth adding to the routine: never say "not supported by Gmail" — say "no Gmail activity for the stored address <X> — verify address" instead.
